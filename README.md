# Unsupervised-CNN-for-wafer-categorization
The Python implementation for my Bachelor's thesis, latest code changes in December 2024. 
CNN is implemented with PyTorch Conv2D layers, basic linear layers, and ReLU:s.

Some parts, i.e., processing data and the data set are left out on purpose. 
